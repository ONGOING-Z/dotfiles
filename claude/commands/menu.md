请向用户展示以下常用操作列表，等用户选择后执行：

1. **commit** — 查看当前变更, 生成 conventional commit message, 提交
2. **pr** — 基于当前分支创建 Pull Request
3. **review** — 审查当前分支的未提交变更
4. **release** — 发布新版本（打 tag + push）
5. **clean** — 清理无用文件、临时分支
6. **update** — 更新 dotfiles（git pull + ./install）
7. **health** — 运行健康检查

请用紧凑的格式展示编号和名称，让用户输入数字或名称来选择。
$ARGUMENTS
